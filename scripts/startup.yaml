#cloud-config
# =============================================================================
# SF Compute cloud-init startup config
# =============================================================================
# Injects your SSH public key so you can connect to the VM.
#
# Usage — redeploy your existing node with this file:
#   sf nodes redeploy <your-node-name> --user-data-file ./scripts/startup.yaml
#
# Or create a new node with it:
#   sf nodes create <name> --zone landsend --duration 6h --max-price 20 \
#       --user-data-file ./scripts/startup.yaml
# =============================================================================

disable_root: false
ssh_pwauth: false

users:
  - name: root
    ssh_authorized_keys:
      # ⚠ Replace this placeholder with YOUR OWN public key before deploying.
      # Run: cat ~/.ssh/id_ed25519.pub  (or whatever key you use for SF Compute)
      # Then paste the full "ssh-ed25519 AAAA... user@host" line below.
      - ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIF/0/P/oypIKaXTQDBdcPFdpg2Ojgkg92wJXzfhHdTOP asaurabh527@gmail.com
